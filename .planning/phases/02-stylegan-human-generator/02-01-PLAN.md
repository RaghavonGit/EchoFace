---
phase: 02-stylegan-human-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - generator/__init__.py
  - generator/stylegan_wrapper.py
  - tests/test_stylegan_wrapper.py
  - .gitignore
autonomous: true
requirements:
  - GEN-01

must_haves:
  truths:
    - "stylegan_wrapper.py loads the .pkl using legacy.load_network_pkl(), not raw pickle.load()"
    - "G parameters all have requires_grad=False after loading"
    - "synthesize() calls G.synthesis(w, noise_mode='const', force_fp32=True)"
    - "generator/ directory is a proper Python package (importable)"
    - "ori_model.txt side-effect is gitignored"
  artifacts:
    - path: "generator/__init__.py"
      provides: "Python package marker — enables 'from generator.stylegan_wrapper import StyleGANWrapper'"
    - path: "generator/stylegan_wrapper.py"
      provides: "StyleGANWrapper class with __init__, sample_w, synthesize, generate_and_save"
      exports: ["StyleGANWrapper"]
    - path: "tests/test_stylegan_wrapper.py"
      provides: "Test stubs for GEN-01, GEN-02, GEN-03 — all initially skip-guarded"
    - path: ".gitignore"
      provides: "Excludes ori_model.txt and __pycache__ from version control"
  key_links:
    - from: "generator/stylegan_wrapper.py"
      to: "StyleGAN-Human/legacy.py"
      via: "sys.path.insert(0, _SG_ROOT) then import legacy"
      pattern: "sys\\.path\\.insert.*StyleGAN-Human"
    - from: "generator/stylegan_wrapper.py"
      to: "gpu_utils.get_device_config()"
      via: "cfg dict consumed in __init__"
      pattern: "cfg\\['device'\\]"
---

<objective>
Create the generator package scaffold, test stubs, and implement the StyleGANWrapper class with correct pkl loading and parameter freezing.

Purpose: GEN-01 requires legacy.load_network_pkl() for safe pkl deserialization and requires_grad=False on G parameters before Phase 3 can use the generator in an optimization loop.
Output: generator/__init__.py, generator/stylegan_wrapper.py (StyleGANWrapper class), tests/test_stylegan_wrapper.py (stubs), .gitignore entry for ori_model.txt.
</objective>

<execution_context>
@C:/Users/ragha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ragha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stylegan-human-generator/02-RESEARCH.md
@.planning/phases/02-stylegan-human-generator/02-VALIDATION.md
@gpu_utils.py

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From gpu_utils.py (Phase 1 deliverable):
```python
def get_device_config() -> dict:
    """
    Returns dict with keys:
        device        (torch.device) -- 'cuda' or 'cpu'
        dtype         (torch.dtype)  -- torch.float16 on CUDA, torch.float32 on CPU
        use_fp16      (bool)         -- True on CUDA only
        resolution    (int)          -- 1024 on CUDA, 256 on CPU
        truncation_psi (float)       -- 1.0 on CUDA, 0.5 on CPU
    """
```

From StyleGAN-Human/legacy.py (confirmed on disk):
```python
def load_network_pkl(f, force_fp16=False) -> dict:
    """Returns dict; key 'G_ema' is the EMA generator network."""
```

From StyleGAN-Human/dnnlib/util.py (confirmed on disk, lines 389-391):
```python
# open_url(path): if path has no '://' scheme, falls through to open(path, 'rb')
# Plain Windows backslash paths are safe — no '://' present
with dnnlib.util.open_url(pkl_path) as f:
    G = legacy.load_network_pkl(f)['G_ema'].to(device)
```

Side-effect: legacy.load_network_pkl() writes ori_model.txt to CWD unconditionally.
This cannot be disabled without modifying StyleGAN-Human/ (off-limits).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold, package marker, and .gitignore</name>
  <files>tests/test_stylegan_wrapper.py, generator/__init__.py, .gitignore</files>
  <action>
    Create three files:

    **generator/__init__.py** — empty file. Its sole purpose is making generator/ a Python package so `from generator.stylegan_wrapper import StyleGANWrapper` works from the project root.

    **.gitignore** — create (or append if already exists) with entries:
    ```
    ori_model.txt
    __pycache__/
    *.pyc
    .pytest_cache/
    outputs/*.png
    ```
    The ori_model.txt entry is critical — legacy.load_network_pkl() writes this file unconditionally to CWD; it must not be committed.

    **tests/test_stylegan_wrapper.py** — write the complete test stub file. Every test must be implemented with a skip guard initially so the suite is importable and runnable before stylegan_wrapper.py exists. Use class TestStyleGANWrapper. Tests to stub out (all initially raise `pytest.skip("not implemented")`):

    For GEN-01 (these run without CUDA; mock the pkl load):
    - test_load_uses_legacy: verifies legacy.load_network_pkl is called, not pickle.load
    - test_parameters_frozen: verifies all G.parameters() have requires_grad=False after load
    - test_synthesis_signature: verifies G.synthesis called with noise_mode='const' and force_fp32=True

    For GEN-02 (skip if not torch.cuda.is_available()):
    - test_gpu_sample_w_shape: verifies sample_w() returns tensor shape [1, num_ws, 512] on CUDA device
    - test_gpu_generates_1024_png: verifies generate_and_save() writes a valid PIL-openable 1024x1024 PNG to outputs/

    For GEN-03 (CPU path, mock G.synthesis to return [1, 3, 1024, 1024] float tensor):
    - test_cpu_resize_to_256: verifies synthesize() output is 256x256 when cfg['resolution']=256
    - test_cpu_generates_256_png: verifies generate_and_save() writes a valid 256x256 PNG without error

    Import structure at top of test file:
    ```python
    import pytest
    import torch
    from unittest.mock import patch, MagicMock
    ```

    All skip guards must be `pytest.skip("not implemented")` — NOT `pytest.mark.skip` (inline skip, not decorator, so the test is enumerated and can be verified by count).
  </action>
  <verify>
    <automated>cd D:/EchoFace && conda run -n stylehuman python -m pytest tests/test_stylegan_wrapper.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects all 7 tests from test_stylegan_wrapper.py with status "SKIPPED" for each; suite exits 0 (skips are not failures). generator/__init__.py exists. .gitignore contains ori_model.txt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement StyleGANWrapper — loading, freezing, and synthesis signature</name>
  <files>generator/stylegan_wrapper.py</files>
  <behavior>
    - test_load_uses_legacy: StyleGANWrapper.__init__ calls legacy.load_network_pkl (mock-verifiable via unittest.mock.patch)
    - test_parameters_frozen: After StyleGANWrapper.__init__, iterating G.parameters() yields no tensor with requires_grad=True
    - test_synthesis_signature: StyleGANWrapper.synthesize(w) calls self.G.synthesis(w, noise_mode='const', force_fp32=True) — verifiable by mocking G.synthesis
  </behavior>
  <action>
    Create generator/stylegan_wrapper.py with the full StyleGANWrapper class. Follow the RESEARCH.md architecture exactly.

    **sys.path injection (MUST be first, before any dnnlib/legacy import):**
    ```python
    import sys, os
    _SG_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'StyleGAN-Human'))
    if _SG_ROOT not in sys.path:
        sys.path.insert(0, _SG_ROOT)
    import dnnlib
    import legacy
    ```

    **Imports after sys.path:**
    torch, torch.nn.functional as F, PIL.Image, numpy as np, datetime.datetime, gpu_utils

    **StyleGANWrapper class:**

    `__init__(self, pkl_path: str, cfg: dict)`:
    - Store `self.device = cfg['device']`, `self.dtype = cfg['dtype']`, `self.resolution = cfg['resolution']`, `self.truncation_psi = cfg['truncation_psi']`
    - Load G: `with dnnlib.util.open_url(os.path.abspath(pkl_path)) as f: self.G = legacy.load_network_pkl(f)['G_ema'].to(self.device)`
    - Set eval mode: `self.G = self.G.eval()`
    - Freeze ALL parameters: `for p in self.G.parameters(): p.requires_grad_(False)`
    - Docstring must note: "Side-effect: legacy.load_network_pkl() writes ori_model.txt to CWD. This cannot be disabled without modifying StyleGAN-Human/ (off-limits). File is gitignored."

    `sample_w(self) -> torch.Tensor`:
    - Returns W+ latent shape [1, G.num_ws, 512]
    - Implementation: `z = torch.randn(1, self.G.z_dim, device=self.device); label = torch.zeros([1, self.G.c_dim], device=self.device); return self.G.mapping(z, label, truncation_psi=self.truncation_psi)`

    `synthesize(self, w: torch.Tensor) -> torch.Tensor`:
    - Args: w shape [1, num_ws, 512]
    - Returns: [H, W, 3] uint8 tensor on CPU, where H=W=self.resolution
    - Implementation:
      1. `img = self.G.synthesis(w, noise_mode='const', force_fp32=True)` — force_fp32=True is MANDATORY
      2. `img = (img.permute(0, 2, 3, 1) * 127.5 + 128).clamp(0, 255).to(torch.uint8)` — exact formula from generate.py
      3. `img_hwc = img[0].cpu()` — shape [H, W, 3]
      4. If self.resolution != img_hwc.shape[0]: resize using F.interpolate on permuted CHW tensor, then permute back to HWC
      5. Return img_hwc

    `generate_and_save(self, out_dir: str = 'outputs') -> str`:
    - Calls sample_w(), synthesize(), saves PNG
    - PNG save: `os.makedirs(out_dir, exist_ok=True)`, timestamp = `datetime.datetime.now().strftime("%Y%m%d_%H%M%S_%f")`, path = `os.path.join(out_dir, f"gen_{timestamp}.png")`, `PIL.Image.fromarray(img_hwc.numpy(), 'RGB').save(path)`, return path

    After implementing, update test_stylegan_wrapper.py to replace the skip guards in test_load_uses_legacy, test_parameters_frozen, and test_synthesis_signature with actual implementations using unittest.mock.

    For test_load_uses_legacy: patch 'generator.stylegan_wrapper.legacy.load_network_pkl' and 'generator.stylegan_wrapper.dnnlib.util.open_url', assert load_network_pkl was called.
    For test_parameters_frozen: use a MagicMock for G whose parameters() returns tensors with requires_grad=False; assert no parameter has requires_grad=True after __init__.
    For test_synthesis_signature: mock self.G.synthesis, call wrapper.synthesize(w_mock), assert mock called with noise_mode='const' and force_fp32=True.

    NOTE: All three GEN-02 and GEN-03 tests remain as pytest.skip("not implemented") — those are implemented in Plan 02.
  </action>
  <verify>
    <automated>cd D:/EchoFace && conda run -n stylehuman python -m pytest tests/test_stylegan_wrapper.py::TestStyleGANWrapper::test_load_uses_legacy tests/test_stylegan_wrapper.py::TestStyleGANWrapper::test_parameters_frozen tests/test_stylegan_wrapper.py::TestStyleGANWrapper::test_synthesis_signature -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>test_load_uses_legacy, test_parameters_frozen, and test_synthesis_signature all PASS. The four GPU/CPU tests remain SKIPPED. Full suite exits 0.</done>
</task>

</tasks>

<verification>
Run full test suite after both tasks complete:

```
cd D:/EchoFace && conda run -n stylehuman python -m pytest tests/test_stylegan_wrapper.py -v --tb=short
```

Expected: 3 PASSED, 4 SKIPPED, 0 FAILED, exit code 0.

Also verify package import works from project root:
```
cd D:/EchoFace && conda run -n stylehuman python -c "from generator.stylegan_wrapper import StyleGANWrapper; print('import OK')"
```
Expected: "import OK" with no errors (does NOT load the pkl — just imports the class).
</verification>

<success_criteria>
- generator/__init__.py exists (empty package marker)
- generator/stylegan_wrapper.py exists with StyleGANWrapper class
- tests/test_stylegan_wrapper.py has 7 tests; 3 pass (GEN-01), 4 skip (GEN-02/03 pending)
- .gitignore contains ori_model.txt
- `from generator.stylegan_wrapper import StyleGANWrapper` imports without error from project root
- GEN-01: pkl loading uses legacy.load_network_pkl (verified by mock test)
- GEN-01: all G parameters frozen after load (verified by mock test)
- GEN-01: synthesis call signature correct — noise_mode='const', force_fp32=True (verified by mock test)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stylegan-human-generator/02-01-SUMMARY.md`
</output>
