---
phase: 06-gradio-ui-integration
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_gradio_app.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - CLIP-03

must_haves:
  truths:
    - "8 failing tests exist in tests/test_gradio_app.py covering generate_fn() and export_image()"
    - "Tests import generate_fn and export_image as standalone functions (not module-level Gradio objects)"
    - "All 8 tests fail with NotImplementedError or ImportError — not with syntax or test-logic errors"
  artifacts:
    - path: "tests/test_gradio_app.py"
      provides: "RED test scaffold for UI-01, UI-02, CLIP-03"
      exports:
        - TestGenerate
        - TestExport
  key_links:
    - from: "tests/test_gradio_app.py"
      to: "ui/app.py"
      via: "from ui.app import generate_fn, export_image"
      pattern: "from ui\\.app import"
---

<objective>
Create the RED test scaffold for Phase 6. Eight unit tests covering all three requirements (UI-01, UI-02, CLIP-03) must fail with ImportError before ui/app.py exists, then turn GREEN when Plan 02 implements the functions.

Purpose: Nyquist compliance — every implementation task in Plans 02 has an automated verify command that runs in < 60 seconds against this test file.
Output: tests/test_gradio_app.py with 8 test methods, all RED.
</objective>

<execution_context>
@C:/Users/ragha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ragha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-gradio-ui-integration/06-CONTEXT.md
@.planning/phases/06-gradio-ui-integration/06-RESEARCH.md

<interfaces>
<!-- Existing module APIs that generate_fn and export_image will call. -->
<!-- Tests mock all of these — executors must match these signatures exactly. -->

From optimizer/clip_optimizer.py:
  optimize(text_prompt: str, wrapper, cfg: dict, callback=None) -> (PIL.Image, w_tensor, float)
  # callback signature: callback(step: int, loss: float, sim_score: float)

From asr/audio_processor.py:
  process_audio(path: str) -> np.ndarray  # 16kHz mono float32

From asr/transcriber.py:
  Transcriber(cfg: dict).transcribe(audio_array: np.ndarray) -> str

From gpu_utils.py:
  get_device_config() -> dict  # keys: device, dtype, use_fp16, resolution, truncation_psi

From generator/stylegan_wrapper.py:
  StyleGANWrapper(pkl_path: str, cfg: dict)
  .sample_w() -> Tensor[1, 18, 512]
  .synthesize(w) -> Tensor[H, W, 3] uint8

Target functions to test (to be created in ui/app.py):
  generate_fn(mic_audio, file_audio, text_input, wrapper, cfg) -> generator
    yields tuples: (status: str, text_in_update: str, score: str, image: np.ndarray|None, pil_image: PIL.Image|None, export_btn_update)
    NOTE: text_in_update is the 2nd element — the ASR path fills it; text path and optimization yields pass "" for this slot.

  export_image(pil_image, outputs_dir: str) -> (gr.update, gr.update)
    saves pil_image to outputs_dir/YYYYMMDD_HHMMSS.png
    returns (gr.update(visible=True), gr.update(visible=True, value=path))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test scaffold for generate_fn and export_image</name>
  <files>tests/test_gradio_app.py</files>
  <behavior>
    TestGenerate class (6 tests):
    - test_text_bypass: when text_input="a young woman", generate_fn never calls process_audio or Transcriber; final yielded status is "Done."
    - test_asr_path: when text_input="" and file_audio="/tmp/x.wav", generate_fn calls process_audio("/tmp/x.wav") and Transcriber(cfg).transcribe(array); the FIRST yield has text_in_update (index 1 of the tuple) equal to the transcription string; the FIRST yield has status (index 0) containing "review and edit"; generate_fn returns WITHOUT calling optimize() (mock_opt.call_count == 0 after the generator is exhausted)
    - test_empty_input: when mic_audio=None, file_audio=None, text_input="", first yielded status contains "Please provide a description or record audio."
    - test_exception_handling: when optimize() raises RuntimeError("out of memory"), generate_fn yields status containing "Generation failed:" and "out of memory"
    - test_score_yielded: during optimization with a callback that fires at step 10 with sim=0.42, generate_fn yields a score string containing "0.42" before the final yield
    - test_final_score: final yielded tuple has score string containing final_sim formatted to 4 decimal places, image is np.ndarray, export_btn_update is gr.update(visible=True)

    TestExport class (2 tests):
    - test_export_saves_file: export_image(pil_image, tmp_dir) saves a file in tmp_dir matching pattern YYYYMMDD_HHMMSS.png; file exists on disk
    - test_export_shows_path: export_image(pil_image, tmp_dir) returns tuple where second element is gr.update with visible=True and value equal to the saved path

    CRITICAL for test_asr_path — the two-stage UX flow:
    The locked decision (CONTEXT.md) requires that when text is empty and audio is provided, generate_fn fills the text box with the transcription and STOPS — it does NOT call optimize(). The user then reviews the transcription in text_in, edits if needed, and clicks Generate again. On the second click text_in is non-empty so the text-bypass path runs optimize().

    test_asr_path must assert ALL of the following:
    1. process_audio was called with the audio file path
    2. Transcriber(cfg).transcribe() was called with the returned audio array
    3. The first (and only) yielded tuple's index-1 element equals the mock transcription string
    4. The first (and only) yielded tuple's index-0 (status) contains "review" or "edit" or "Transcription complete"
    5. mock_opt.call_count == 0  (optimize() was NOT called)

    This test is the acceptance gate for the two-stage flow.
  </behavior>
  <action>
    Create tests/test_gradio_app.py.

    CRITICAL TESTABILITY DESIGN — generate_fn must accept wrapper and cfg as explicit parameters (not close over module-level globals). This makes it mockable without importing the full module. Tests import generate_fn and export_image directly; they do NOT import the Gradio demo object.

    Test file structure:

    ```python
    import sys, os
    sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

    import unittest
    import tempfile
    import numpy as np
    import torch
    from PIL import Image
    from unittest.mock import patch, MagicMock, call

    from ui.app import generate_fn, export_image
    ```

    Helper builders (reuse pattern from test_clip_optimizer.py):
    - _make_mock_wrapper(): MagicMock with sample_w, synthesize, G.synthesis attributes
    - _make_mock_cfg(): dict with device='cpu', dtype=torch.float32, use_fp16=False, resolution=256, truncation_psi=0.5
    - _make_pil_image(): PIL.Image.new('RGB', (256, 512), color=(128, 64, 32)) — 256x512 portrait matching StyleGAN-Human v2 output

    For test_score_yielded and test_final_score: mock optimize() using a side_effect that:
    1. Calls the callback arg at step=10 with loss=0.5, sim_score=0.42
    2. Returns (pil_image, MagicMock(), 0.7777)

    To simulate the threading pattern in generate_fn, the mock optimize() must actually call the callback synchronously from within the mock so the queue.Queue bridge works end-to-end. Use side_effect:

    ```python
    def _fake_optimize(prompt, wrapper, cfg, callback=None):
        if callback:
            callback(10, 0.5, 0.42)
        return (pil_image, MagicMock(), 0.7777)
    mock_opt.side_effect = _fake_optimize
    ```

    For test_asr_path: mock process_audio to return a dummy np.ndarray; mock Transcriber so
    Transcriber(cfg).transcribe(array) returns "a young woman with dark hair". Exhaust the generator
    with list(generate_fn(None, "/tmp/x.wav", "", wrapper, cfg)) — collect all yielded tuples.
    Then assert:
      - mock_process_audio.called and mock_process_audio.call_args[0][0] == "/tmp/x.wav"
      - mock_transcriber_instance.transcribe.called
      - len(yields) == 1  (generator yields exactly once then stops)
      - yields[0][1] == "a young woman with dark hair"  (text_in_update slot)
      - "review" in yields[0][0].lower() or "edit" in yields[0][0].lower() or "transcription" in yields[0][0].lower()
      - mock_opt.call_count == 0

    For test_export_saves_file and test_export_shows_path: use tempfile.mkdtemp() as outputs_dir to avoid touching the real outputs/ directory.

    Patch namespaces (follow established project pattern — patch the module's namespace binding):
    - patch('ui.app.optimize', ...) for optimize calls
    - patch('ui.app.process_audio', ...) for ASR path
    - patch('ui.app.Transcriber', ...) for Transcriber class

    All tests must FAIL on first run (ui/app.py does not exist yet → ImportError). Do not add any try/except around the import at module level — let it fail cleanly so the RED state is clear.

    After writing the file, confirm RED state:
    conda run -n stylehuman python -m pytest tests/test_gradio_app.py -x -q 2>&1 | head -20
    Expected: ImportError or ModuleNotFoundError, exit code non-zero.
  </action>
  <verify>
    <automated>conda run -n stylehuman python -m pytest tests/test_gradio_app.py -x -q 2>&1 | head -5</automated>
  </verify>
  <done>tests/test_gradio_app.py exists with 8 test methods across TestGenerate and TestExport. Running pytest returns non-zero exit (ImportError on ui.app) — RED state confirmed. No syntax errors in the test file itself (python -m py_compile tests/test_gradio_app.py exits 0).</done>
</task>

</tasks>

<verification>
conda run -n stylehuman python -m py_compile tests/test_gradio_app.py
conda run -n stylehuman python -m pytest tests/test_gradio_app.py --collect-only -q
# Expected: 8 tests collected, 0 errors in collection (ImportError deferred to runtime)
</verification>

<success_criteria>
- tests/test_gradio_app.py exists
- 8 test methods collected by pytest
- File compiles cleanly (no syntax errors)
- Running tests produces ImportError or ModuleNotFoundError for ui.app — RED state
- No test passes prematurely
- test_asr_path specifically asserts: transcription fills text_in (yield tuple index 1), status signals user to review, optimize() is NOT called (call_count == 0)
</success_criteria>

<output>
After completion, create .planning/phases/06-gradio-ui-integration/06-01-SUMMARY.md
</output>
