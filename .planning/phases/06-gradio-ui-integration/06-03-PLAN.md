---
phase: 06-gradio-ui-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - CLIP-03

must_haves:
  truths:
    - "Running `python ui/app.py` opens http://127.0.0.1:7860 in the browser with no traceback"
    - "Typing a description and clicking Generate produces a face image in the UI"
    - "CLIP similarity score text updates during the 150-step optimization"
    - "Export PNG button appears after generation and saves a timestamped file to outputs/"
    - "CPU-only startup notice appears when CUDA is unavailable"
  artifacts:
    - path: "ui/app.py"
      provides: "Verified running Gradio app"
  key_links:
    - from: "browser at http://127.0.0.1:7860"
      to: "ui/app.py generate_fn()"
      via: "Generate button click"
      pattern: "Done\\."
---

<objective>
Human-verify the full end-to-end pipeline: launch the Gradio app, provide a typed description, watch the CLIP score update, confirm the generated face appears, and export it as PNG. This is the Phase 6 acceptance gate.

Purpose: Automated tests verify function logic in isolation — this checkpoint verifies the assembled system works as a user would experience it.
Output: Confirmed working EchoFace Gradio UI.
</objective>

<execution_context>
@C:/Users/ragha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ragha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-gradio-ui-integration/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Launch app and confirm server starts</name>
  <files></files>
  <action>
    From the project root (D:/EchoFace), run:
      conda run -n stylehuman python ui/app.py

    Run this in the background. Wait for the log line:
      "Running on local URL:  http://127.0.0.1:7860"

    If the server does not start within 60 seconds, check for:
    - ImportError → fix missing sys.path injection in ui/app.py
    - StyleGANWrapper pkl load failure → verify PKL_PATH is absolute and correct
    - Gradio version error (sources= instead of source=) → fix to Gradio 3.x API

    The server startup loads StyleGAN pkl (~5s) and CLIP model (~3s). Total startup expected under 30s.

    Confirm the server is listening:
      curl -s http://127.0.0.1:7860/ | head -5
    Expected: HTML response (not connection refused).
  </action>
  <verify>
    <automated>conda run -n stylehuman python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Server responds to HTTP requests at http://127.0.0.1:7860. Full test suite still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full EchoFace Gradio UI with:
    - Mic record button (gr.Audio source="microphone")
    - WAV/MP3 file upload (gr.Audio source="upload")
    - Text description override box (bypasses ASR when non-empty)
    - Generate button wired to optimize() via threading + queue.Queue bridge
    - Live CLIP similarity score updating every 10 steps
    - Generated face image display (512x1024 on GPU, 256x256 on CPU)
    - Export PNG button (appears only after generation)
    - Saved file path display after export
    - CPU-only startup notice if no CUDA
  </what-built>
  <how-to-verify>
    Open http://127.0.0.1:7860 in your browser.

    Test 1 — Text input path (fastest, no mic needed):
    1. Leave mic and file upload empty
    2. Type "a young woman with dark hair and blue eyes" in the text box
    3. Click Generate
    4. Observe: status shows "Optimizing... step 10/150", "Optimizing... step 20/150", etc.
    5. Observe: CLIP Similarity Score field updates with each step
    6. Wait for "Done." status (150 steps, may take 2-10 min depending on GPU/CPU)
    7. Verify: face image appears in Generated Face panel
    8. Verify: CLIP similarity score shows final value (e.g., "CLIP similarity: 0.3142")
    9. Verify: Export PNG button becomes visible
    10. Click Export PNG
    11. Verify: "Saved to" field shows path like "D:\EchoFace\outputs\20260307_153000.png"
    12. Verify: that file exists on disk in the outputs/ directory

    Test 2 — Empty input guard:
    1. Clear the text box
    2. Click Generate (with no audio and no text)
    3. Verify: status shows "Please provide a description or record audio."
    4. Verify: Generate button is still enabled (can retry immediately)

    Test 3 — CPU notice (only if no CUDA):
    - If running on CPU-only machine: verify yellow/bold notice at top: "No GPU detected — generation will be slow (CPU mode, 256x256)"

    Expected signs of success: step counter increments, score updates, image appears, PNG saved.
    Expected signs of failure: blank image, score never updates, Export button never appears, traceback in terminal.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three tests pass.
    Or describe the specific failure (e.g., "score never updated", "export button missing", "crash at step 40").
  </resume-signal>
</task>

</tasks>

<verification>
Human visual verification per checkpoint above.
Automated: conda run -n stylehuman python -m pytest tests/ -x -q (must be green before checkpoint).
</verification>

<success_criteria>
1. Gradio app launches at http://127.0.0.1:7860 with no errors
2. Text-input generation path produces a face image after 150 steps
3. CLIP similarity score updates during optimization (visible in score_out field)
4. Export PNG saves timestamped file to outputs/ and shows path in UI
5. Empty input shows friendly error message and keeps Generate button active
6. Full test suite green (all phases, including Phase 6 tests)
</success_criteria>

<output>
After completion, create .planning/phases/06-gradio-ui-integration/06-03-SUMMARY.md
</output>
