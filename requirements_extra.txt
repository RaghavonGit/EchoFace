# EchoFace — Python dependencies
# Conda env: stylehuman (Python 3.8.x)
# PyTorch + CUDA already installed via conda — do not reinstall here
#
# Install with:
#   pip install -r requirements_extra.txt

# ── ASR ───────────────────────────────────────────────────────────────────
openai-whisper          # Local Whisper ASR (medium model, no API key)
tiktoken==0.5.2         # Whisper tokenizer dependency

# ── CLIP ──────────────────────────────────────────────────────────────────
clip @ git+https://github.com/openai/CLIP.git   # Local CLIP ViT-B/32

# ── Image / Vision ────────────────────────────────────────────────────────
torchvision             # CLIP dependency; TVF used in styleclip_wrapper
Pillow                  # PIL image handling throughout pipeline

# ── Audio ─────────────────────────────────────────────────────────────────
librosa==0.9.2          # Audio preprocessing (resample, trim, normalize)
soundfile>=0.10.3       # Audio file I/O
sounddevice>=0.4.4      # Microphone input for Gradio

# ── UI ────────────────────────────────────────────────────────────────────
gradio>=3.40,<4.0       # Gradio 4.x requires Python 3.10+; pinned to 3.x

# ── Landmarks / Accessories ───────────────────────────────────────────────
face-alignment>=1.4.1   # 68-point 2D facial landmark detection

# ── Numerical ─────────────────────────────────────────────────────────────
numpy<1.24              # numpy >=1.24 breaks librosa and several StyleGAN deps
